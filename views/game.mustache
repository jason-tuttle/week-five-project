<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>MYSTERY WORD :: PLAY!</title>
    <link rel="stylesheet" href="/public/css/styles.css">
  </head>
  <body>
    <header>
      <div class="head left">
        MYSTERY WORD
      </div>
      <div class="head right">
        You have {{game.guessesLeft}} guesses left.
      </div>
    </header>
    <section class="game">
      <div>
        <p class="game-word">
        {{#game.guessWord}}
          {{.}}&nbsp;
        {{/game.guessWord}}
        </p>
        {{#lose}}
          <p class="lose-message">
            The word was: {{game.word}}
          </p>
        {{/lose}}
      </div>
      <div class="game-form">
        <form class="game-form" action="/game" method="post">
          <label for="guess">Guess a Letter:</label>
          <input type="text" id="guess" name="guess" value="">
          <input type="submit" name="submit" value="Make A Guess">
        </form>
      </div>
      <div class="error-message">
        <span>{{error}}</span>
      </div>
      <div class="game-guesses">
        <p>Letters guessed: <span class="guessed">{{#game.guessed}}{{.}} {{/game.guessed}}</span></p>
      </div>
      <div class="">
        <form class="" action="/reset" method="post">
        {{#win}}
            <p class="win-message">{{winLossMessage}}</p>
            <input type="submit" name="playagain" value="Play Again">
        {{/win}}
        {{#lose}}
            <p class="lose-message">{{winLossMessage}}</p>
            <input type="submit" name="playagain" value="Play Again">
        {{/lose}}
        </form>
      </div>
    </section>
  </body>
</html>
